import { CustomeFilterPipe } from './custome-filter.pipe';

describe( 'CustomeFilterPipe', () => {
  it( 'create an instance', () => {
    const pipe = new CustomeFilterPipe();
    expect( pipe ).toBeTruthy();
  } );
} );
